<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.eidos.fleetkick</string>

  <!-- serve.sh runs the bridge as a child and execs ttyd in the foreground, so launchd
       watches ttyd; if either dies the whole pair is rebuilt. -->
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>__DIR__/serve.sh</string>
    <string>__WORKDIR__</string>
  </array>

  <key>RunAtLoad</key>
  <true/>
  <!-- The point of this file: the extension may reconnect whenever it likes, because
       the thing it reconnects to never stays down. -->
  <key>KeepAlive</key>
  <true/>
  <key>ThrottleInterval</key>
  <integer>5</integer>

  <key>StandardOutPath</key>
  <string>/tmp/fleetkick.log</string>
  <key>StandardErrorPath</key>
  <string>/tmp/fleetkick.log</string>

  <!-- ~/.local/bin FIRST and non-negotiable: that is where the claude binary lives. A
       launchd job inherits none of your shell's PATH, so without it boot.sh's `exec claude`
       fails, the session's command exits instantly, tmux discards the session, and the
       daemon looks perfectly healthy while being unable to create a single session. -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>__HOME__/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>
</dict>
</plist>
